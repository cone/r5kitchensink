<%= form_for @product, url: {action: 'update'} do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :description %>
  <%= f.text_field :description %>
  <%= f.submit 'Edit' %>
<% end %>

<!--Valid as well: <%= #form_for @product, url: {action: 'update'} do |f| %>-->